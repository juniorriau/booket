<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Jenistiket extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Jenistiket_model');
        $this->load->library('form_validation');
    }

    public function index()
    {
        $q = urldecode($this->input->get('q', TRUE));
        $start = intval($this->input->get('start'));
        
        if ($q <> '') {
            $config['base_url'] = base_url() . 'jenistiket/index.html?q=' . urlencode($q);
            $config['first_url'] = base_url() . 'jenistiket/index.html?q=' . urlencode($q);
        } else {
            $config['base_url'] = base_url() . 'jenistiket/index.html';
            $config['first_url'] = base_url() . 'jenistiket/index.html';
        }

        $config['per_page'] = 10;
        $config['page_query_string'] = TRUE;
        $config['total_rows'] = $this->Jenistiket_model->total_rows($q);
        $jenistiket = $this->Jenistiket_model->get_limit_data($config['per_page'], $start, $q);

        $this->load->library('pagination');
        $this->pagination->initialize($config);

        $data = array(
            'jenistiket_data' => $jenistiket,
            'q' => $q,
            'pagination' => $this->pagination->create_links(),
            'total_rows' => $config['total_rows'],
            'start' => $start,
        );
        $this->load->view('jenistiket/jenistiket_list', $data);
    }

    public function read($id) 
    {
        $row = $this->Jenistiket_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'jenis' => $row->jenis,
	    );
            $this->load->view('jenistiket/jenistiket_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('jenistiket'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('jenistiket/create_action'),
	    'id' => set_value('id'),
	    'jenis' => set_value('jenis'),
	);
        $this->load->view('jenistiket/jenistiket_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'jenis' => $this->input->post('jenis',TRUE),
	    );

            $this->Jenistiket_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('jenistiket'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Jenistiket_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('jenistiket/update_action'),
		'id' => set_value('id', $row->id),
		'jenis' => set_value('jenis', $row->jenis),
	    );
            $this->load->view('jenistiket/jenistiket_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('jenistiket'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'jenis' => $this->input->post('jenis',TRUE),
	    );

            $this->Jenistiket_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('jenistiket'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Jenistiket_model->get_by_id($id);

        if ($row) {
            $this->Jenistiket_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('jenistiket'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('jenistiket'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('jenis', 'jenis', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Jenistiket.php */
/* Location: ./application/controllers/Jenistiket.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2023-09-02 04:30:09 */
/* http://harviacode.com */